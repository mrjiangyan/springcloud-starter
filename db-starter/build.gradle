def artifactId = 'com.touchbiz.db.starter'

dependencies {
    implementation group: 'mysql', name: 'mysql-connector-java', version: "${ mysqlVerision }"
    implementation group: 'org.springframework', name: 'spring-jdbc', version: "${ springVersion }"
    implementation group: 'com.alibaba', name: 'druid-spring-boot-starter', version: "${ druidVersion }"
    implementation group: 'org.mybatis.spring.boot', name: 'mybatis-spring-boot-starter', version: "${ mybatisVersion }"
    implementation group: 'tk.mybatis', name: 'mapper', version: "${ tkVersion }"
    testImplementation group: 'org.springframework.boot', name: 'spring-boot-starter-test', version: "${ springBootVersion }"
}

