spring:
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: nacos.ai-prime.tech:8848
        namespace: pangqn
      config:
        server-addr: nacos.ai-prime.tech:8848
        namespace: pangqn
        file-extension: yaml
#    loadbalancer:
#      enabled: true






management:
  endpoint:
    web:
      exposure:
        include: '*'